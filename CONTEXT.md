# Edge Controller

自建 CDN 的控制面：一台主控与多台边缘节点，把人的配置意图变成各节点上真实生效的
Caddy 配置，并让每一次变更可预览、可追溯、可回滚。

## Language

### 拓扑

**主控**:
持有全部配置真相、渲染配置、签发证书、编排下发的那一台。
_Avoid_: Master（中文行文里）、服务端、控制器

**边缘节点**:
承载流量的机器，其上跑一个 Agent 与一个官方版 Caddy。是配置的接收方，不是配置的来源。
_Avoid_: 节点机、CDN 节点、Edge

**Agent**:
边缘节点上的常驻进程，与主控保持一条 gRPC 长连接，负责上报心跳、接收并应用配置、回报结果。
它不做决策，只执行与回报。

### 配置

**草稿**:
尚未下发的配置改动，叠加在基线之上。**全局可见**——任何人都能看到别人正在改什么。
_Avoid_: 暂存、pending change

**基线**:
当前应当在所有边缘节点上生效的那一版配置，由最近一次成功下发确立。
_Avoid_: 当前版本、生效配置

**下发**:
把选中的草稿合入基线并广播到各边缘节点的完整过程：校验 → 确认 → 广播 → 逐节点回报 → 确立新基线。
一次下发只携带**本次勾选**的草稿，未勾选的仍是草稿。
_Avoid_: 推送、发布、部署

**快照**:
一次下发所固化的配置全文与结果记录，含版本号、操作人、变更资源、逐节点回报。回滚以它为源。

**可读表示**:
工作台右栏展示的那份 JSON。它是变更的**可读摘要**，不是将要下发的字节——
真实 diff 只在「校验并推送」的确认弹层出现（见 [ADR-0007](docs/adr/0007-workbench-preview-is-a-representation.md)）。
_Avoid_: 预览 JSON、Caddy JSON 预览（这两种叫法会让人以为它可下发）

**配置漂移**:
边缘节点上报的配置版本号 ≠ 当前基线。它回答的是「这次下发到没到」。
**它不检查节点上的配置内容**——有人 SSH 上去手改配置、或节点重启后回退，版本号照旧，漂移不会亮。
要覆盖那种情况需要回读比对，当前不做（见 [ADR-0002](docs/adr/0002-drift-is-version-comparison.md)）。
_Avoid_: 配置不一致、失步

### 准入

**路由**:
一个对外域名到一个回源地址的映射，附带处置方式、请求体上限、压缩等策略。
_Avoid_: 站点、反代规则

**访问规则**:
挂在一个或多个域名上的准入条件（IP 白名单 / 服务密钥 / JWT）。
未绑定域名的规则不生效——那是半成品状态，不是「对所有域名生效」。

**处置方式**:
请求未通过访问规则时的响应方式：静默断连 / 403 / 404。默认静默断连，不暴露服务是否存在。
_Avoid_: 拒绝策略、block action

**回源 mTLS**:
边缘节点在回源时向源站出示 `edge-mtls` 客户端证书，由源站校验。
**不是**「要求访问者出示证书」——两者方向相反，别混用「mTLS」一词指代后者
（见 [ADR-0008](docs/adr/0008-route-mtls-is-upstream-client-cert.md)）。
_Avoid_: 双向认证（单独出现时歧义太大）

**校验端点**:
Agent 在边缘节点回环地址上暴露的鉴权接口，对 JWT 与服务密钥做真实验签。
Caddy 通过 forward_auth 委托给它，并把它回传的声明透传给源站（见 [ADR-0003](docs/adr/0003-edge-auth-via-agent-forward-auth.md)）。
_Avoid_: 鉴权服务、auth sidecar

### 证书

**证书签发**:
由**主控**集中完成（DNS-01），签发结果经 gRPC 隧道下发到各边缘节点。
边缘节点不持有 DNS 服务商凭据，也不自行申请证书（见 [ADR-0001](docs/adr/0001-master-issues-certificates.md)）。

**证书清单**:
Agent 上报的、本机 Caddy 当前真正加载的证书。它是**回执**，不是账本——
主控自己持有全部签发记录，清单回答的是「下发到了之后有没有真的生效」。
_Avoid_: 证书列表、cert inventory（这两种叫法听起来像是权威来源）

**回源 CA**:
签发边缘节点回源时出示的客户端证书（`edge-mtls`），信任方是各源站。叶子 24 小时，
经隧道自动续期；吊销即停止续期。

**隧道 CA**:
签发 Agent 连接主控所用的客户端证书，信任方只有主控。与回源 CA **相互独立**——
共用会让源站侧的一次换根把所有节点踢下控制面（见 [ADR-0009](docs/adr/0009-internal-pki-two-cas.md)）。

**接入**:
一台新节点获得身份的过程：一次性 Token + 服务端单向 TLS 建立首连，主控在这次交换中
签发隧道客户端证书；此后该节点全部连接走 mTLS。Token 用后即失效。
_Avoid_: 注册、上线（「上线」另指恢复 DNS 解析）
